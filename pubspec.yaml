name: modirapp_shop
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <4.0.0"  # Flutter SDK에 맞는 버전으로 수정

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0  # 최신 버전으로 업데이트
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
